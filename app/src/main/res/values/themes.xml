<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BarberiaGLP" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/red_primary</item>
    </style>
    <style name="CustomEditText">
        <item name="android:background">@drawable/inputstyle</item>
        <item name="android:textColor">#33FFFFFF</item>
        <item name="android:textColorHint">#AAAAAA</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="BotonesStyle">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:buttonCornerRadius">6dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Theme.BarberiaGLP" parent="Base.Theme.BarberiaGLP" />
</resources>